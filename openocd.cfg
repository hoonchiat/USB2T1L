# OpenOCD configuration for STM32F407 via ST-LINK.
# Flash with:  make flash    (or)   openocd -f openocd.cfg -c "program build/*.elf verify reset exit"
source [find interface/stlink.cfg]
transport select hla_swd
source [find target/stm32f4x.cfg]

reset_config srst_only srst_nogate
